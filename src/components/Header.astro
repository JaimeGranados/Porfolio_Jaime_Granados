---
import ThemeToggle from "./ThemeToggle.astro"

const navItems = [
  {
    titleEs: "Proyectos",
    titleEn: "Projects",
    label: "proyectos",
    url: "/#proyectos",
  },
  {
    titleEs: "Experiencia",
    titleEn: "Experience",
    label: "experiencia",
    url: "/#experiencia",
  },
  {
    titleEs: "Sobre mí",
    titleEn: "About me",
    label: "sobre-mi",
    url: "/#sobre-mi",
  },
  {
    titleEs: "Contacto",
    titleEn: "Contact",
    label: "contacto",
    url: "https://mail.google.com/mail/?view=cm&fs=1&to=jaimealfonsogranados3@gmail.com",
  },
]
---

<header
  class="fixed top-0 z-10 flex items-center justify-center w-full mx-auto mt-4"
>
  <nav
    class="header-nav flex gap-x-1 px-4 py-1.5 text-sm font-medium rounded-full text-gray-600 dark:text-gray-300 justify-center items-center border border-transparent"
  >
    {
      navItems.map((link) => (
        <a
         class="nav-link relative text-xs sm:text-sm block px-1.5 sm:px-3 py-2 sm:py-1.5 rounded-full transition-colors duration-200 hover:text-red-500 dark:hover:text-red-400"
          aria-label={link.label}
          href={link.url}
          target={link.label === "contacto" ? "_blank" : undefined}
          data-es={link.titleEs}
          data-en={link.titleEn}
        >
          {link.titleEs}
        </a>
      ))
    }
    <div class="ml-2 pl-2 border-l border-gray-300/30 dark:border-white/10 flex items-center gap-2">
      <button
        id="lang-toggle"
        type="button"
        class="text-xs font-semibold px-2 py-1 rounded-full transition-colors hover:text-red-500 dark:hover:text-red-400"
        aria-label="Cambiar idioma / Switch language"
      >
        EN
      </button>
      <ThemeToggle />
    </div>
  </nav>
</header>

<script>
  document.addEventListener("astro:page-load", () => {
    const sections = document.querySelectorAll("section")
    const navItems = document.querySelectorAll("header nav a")

    const callback = (entries: any[]) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          navItems.forEach((item) => {
            if (item.getAttribute("aria-label") == entry.target.id) {
              item.classList.add("text-red-500", "dark:text-red-400", "bg-red-400/10")
              item.classList.remove("text-gray-600", "dark:text-gray-300")
            } else {
              item.classList.remove("text-red-500", "dark:text-red-400", "bg-red-400/10")
            }
          })
        }
      })
    }

    const observer = new IntersectionObserver(callback, {
      root: null,
      rootMargin: "0px",
      threshold: 0.3,
    })

    sections.forEach((section) => {
      observer.observe(section)
    })

    document.onvisibilitychange = () => {
      if (document.visibilityState === "hidden") {
        observer.disconnect()
      } else {
        sections.forEach((section) => {
          observer.observe(section)
        })
      }
    }
  })
</script>

<style>
  .header-nav {
    animation: nav-appear 1s linear both;
    animation-timeline: scroll();
    animation-range: 0 100px;
  }

  .header-nav {
    @apply dark:bg-gray-900/80 bg-white/60;
  }

  .nav-link.text-yellow-500 {
    @apply font-semibold;
  }

  @keyframes nav-appear {
    0% {
      @apply dark:bg-gray-900/0 bg-white/0 border-transparent shadow-none;
    }
    to {
      @apply shadow-lg ring-1 backdrop-blur-md ring-white/10 dark:ring-white/5 border-gray-200/20 dark:border-white/5;
    }
  }
</style>
