---
import LinkInline from "./LinkInline.astro"
import ArrowRight from "./icons/ArrowRight.astro"

interface Props {
  titleEs: string
  titleEn: string
  company: string
  descriptionEs: string
  descriptionEn: string
  link?: string
  dateEs: string
  dateEn: string
  current?: boolean
}

const { titleEs, titleEn, company, descriptionEs, descriptionEn, link, dateEs, dateEn, current = false } = Astro.props
---

<div
  class="relative mx-12 pb-12 grid before:absolute before:left-[-35px] before:block before:h-full before:border-l-2 before:border-black/20 dark:before:border-white/15 before:content-[''] md:grid-cols-5 md:gap-10 md:space-x-4]"
>
  <div class="relative pb-12 md:col-span-2">
    <div class="sticky top-0">
      <!-- Title row with dot: flex ensures perfect vertical centering -->
      <div class="flex items-center">
        <span
          class="absolute -left-[42px] flex size-[14px] items-center justify-center"
          aria-hidden="true"
        >
          {current && (
            <span class="animate-ping motion-reduce:animate-none absolute inline-flex size-full rounded-full bg-indigo-500/40"></span>
          )}
          <span
            class={`relative inline-flex size-[10px] rounded-full bg-indigo-500 ${current ? "shadow-[0_0_8px_2px_rgba(99,102,241,0.45)]" : ""}`}
          ></span>
        </span>
        <h3 class="text-xl font-bold text-indigo-500 dark:text-indigo-400" data-es={titleEs} data-en={titleEn}>{titleEs}</h3>
      </div>
      <h4 class="font-semibold text-xl text-gray-600 dark:text-white">{company}</h4>
      <time class="p-0 m-0 text-sm text-gray-600/80 dark:text-white/80" data-es={dateEs} data-en={dateEn}>{dateEs}</time>
    </div>
  </div>
  <div class="relative flex flex-col gap-2 pb-4 text-gray-600 dark:text-gray-300 md:col-span-3">
    <span data-es={descriptionEs} data-en={descriptionEn}>{descriptionEs}</span>
    {
      link && (
        <LinkInline href={link}>
          Saber más sobre {company}{" "}
          <ArrowRight class="size-5" aria-hidden="true" />
        </LinkInline>
      )
    }
  </div>
</div>
